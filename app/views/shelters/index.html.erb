<table class="table">
  <th>Shelter:</th>
  <th>Update?</th>
  <th>Delete?</th>
  <% @shelters.each do |shelter| %>
    <tr>
      <td><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %></td>
      <td><%= link_to "Edit", "shelters/#{shelter.id}/edit" %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Shelter', '/shelters/new' %>
